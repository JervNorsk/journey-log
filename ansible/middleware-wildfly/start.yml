---
- name: "Starting service"
  hosts: wildfly
  collections:
    - middleware_automation.wildfly
  tasks:
    - name: "Start for Wildfly instance"
      shell:
#        cmd: /opt/wildfly-28.0.0.Final/bin/standalone.sh -b 0.0.0.0 -bmanagement 0.0.0.0 --debug & > /tmp/wildfly.pid
        cmd: systemctl start wildfly
